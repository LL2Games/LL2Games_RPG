# 📝 팀 회의록

**날짜: 2026-02-18**
**작성자: gunoo22**

---

## 1. 작업내용

### geonwule
1. 테스터기 레지스트리 IP/PORT 저장방식으로 업데이트, Recv얼러트 삭제
    - Discussion #22에 업데이트파일 업로드함.
2. 몬스터 이동 로직 구현
    - ChannelServer.cpp 내 MapManager::Start를 통해 스레드로 매 2초간격으로 모든 맵에대해 업데이트
    - AttackPacket내 CombatService 활성화
    - MapInstance.cpp 내 InitSpawnMonster 활성화
    - MapManager::Update -> MapUpdateTask -> MapInstance::Update -> Monster::Update 수행 (스레드)
    - Common/PacketParser내 로그 주석처리
    - data/Monster/100101.json 추가(임시)
3. 맵에 입장시 몬스터 로드하다 서버 터지는현상 해결
    - MapInstance.cpp내 MapInstance::InitSpawnMonster함수에서 for문으로 iter를 돌때 ++하는 이터변수를 엉뚱한변수로해서
    무한반복됬었음
    - m_monsterListIter -> m_monsterSpawnListIter로변경
4. 맵안에서 클라이언트 접속종료시 update패킷 날리는현상 해결
    - 플레이어접속종료시 ChannelServer::OnDisconnect 호출하여 m_sessions(ChannelSession)의 delete를 호출
    - ~ChannelSession소멸자 부분에서 Player추출, Map추출해서 OnLeave를 호출하였음
    - MapInstance::Update부분에서 사용자가 있을경우에만 SpawnMonster/UpdateMonster/BroadcastMapInfo 수행
5. 몬스터 막타친 플레이어에대한 Chase모드 추가
    - Monster::OnDamage에서 Attacker를 멤버변수 Player객체로 저장
    - OnDamage에서 E_Chase 모드로 전환
    - Monster::UpdateChase에서 막타플레이어와의 방향을 계산하여 x축을 해당 플레이어 dir로 전환
    - Monster::UpdateChase에서 Player 즉 막타플레이어 객체가 없을경우 예외처리
    - Monster::UpdateChase에서 Player 즉 막타플레이어 객체가 같은 맵에 없을경우 예외처리
        - 이를 구현하기 위해 Monster에 MapId를 추가함.

### dyddlswogh 
1. 플레이어 공격 
  - ComBatService.h :
     - ComputeHitMonsters 함수 구현 : 플레이어의 위치와 몬스터의 위치를 바탕으로 피격당한 몬스터들을 구하는 함수, 피격 당한 몬스터들을 전부 모은 뒤 그중에서 플레이어와 가장 가까운 순으로 피격 몬스터로 설정
     - CalculateSkillBaseDamage 함수 추가 : 플레이어 주스텟, 보조스탯 및 스킬 데미지, 레벨에 따라서 스킬 데미지를 계산하는 함수
     - GetLevelDiffRate 함수 추가 : 플레이어 레벨과 몬스터 레벨의 차이에 따라서 데미지 비율을 설정하는 함수
     - CalculateFinalDamage 함수 추가 : 스킬 데미지와 데미지 비율을 바탕으로 최종 데미지를 계산하는 함수

   - MapInstance.h :
     - ResolveSkillHit 함수 추가 : 피격 당한 몬스터들의 OnDamaged 함수를 실행 BroadcastMonsterHit 함수 실행
     - BroadcastMonsterHit 함수 추가 : OnDamaged에 대한 결과값을 같은 맵의 플레이어들에게 전달하는 함수

   - Monster.h :
     - MonsterHitResult 구조체 추가 : 피격 당한 몬스터들의 정보를 저장하는 구조체 ( 몬스터 구별 ID, 데미지, 현재 체력, 총 체력, 사망 여부)
     - GetCurrentHP / GetMaxHP 함수 추가 : 몬스터의 현재 체력과 총 체력을 반환하는 함수

   - Player.h :
     - GetRootJob 함수 추가 : 플레이어 캐릭터의 0차 직업을 나타내는 enum 값을 반환하는 함수
     - GetSkillLevel 함수 추가 : 플레이어가 배운 스킬의 레벨을 반환하는 함수
     - Root_job 변수형 변환 : int -> enum class로 변환

   - CharacterStat.h :
     - GetMainSubStat 함수 추가 : 캐릭터의 Root_job에 따라서 main stat과 sub stat을 구하는 함수
     - GetStatValue 함수 추가 : 캐릭터의 main stat과 sub stat을 바탕으로 스탯을 반환하는 함수
     - MainSubStat 구조체 추가 : main stat type과 sub stat type을 저장하는 구조체

   - PlayerData.h :
     - RootJob enum 추가 : 플레이어 루트 직업을 나타내는 enum
     - ToRootJob 함수 추가 : 플레이어 정보 DB에서 가져온 INT 값을 바탕으로 enum 루트 직업을 설정하는 함수

   - Pakcet.h :
     - Packet type 추가 :  PKT_MONSTER_MOVE = 0x0F, PKT_MONSTER_ONDAMAGED = 0x10,
2. 아이템 사용
   - ChannelServer.h : ItemManager객체와 ItemService 객체 추가
     - ChannelServer.Init 함수 안에서 ItemManager와 같이 JSON 파일을 읽어오는 클래스들 INIT 실행 하도록 수정

   - ItemManager.h : 싱글톤으로 변경
     - Find 함수와 GetInstacne 함수 추가
       - Item_Id를 바탕으로 읽어온 Item 정보가 있는지 확인하는 함수

   - ItemService 클래스 추가 : 아이템 사용 패킷이 들어왔을 때 처리하는 클래스

   - Player.h : GetCurHP, GetCurMP, CanUseItem, UseItem, AddHP, AddMP, GetItemCount 함수 추가

   - PacketDTO.h 파일 추가 : Packet 수행 후 Send할 때 사용하는 구조체들을 정의하기 위한 헤더파일


## 2. 결정 사항


## 3. Sprint(다음 주까지)

| 담당자 | 작업 내용 | 기한 |
| --- | ----- | -- |
| geonwule   | 몬스터 행동 핸들러 구현  |  다음주 회의시  |
| | - 공격/스킬 | |
| dyddlswogh   | 플레이어 행동 구현 |  다음주 회의시  |
| | - 온데미지 | |



---

## 4. 참고
### Client
![alt text](../res/Client_class.jpg)
![alt text](../res/LL2Games_Tester.jpg)
### Server
![alt text](../res/Server_class.jpg)
![alt text](../res/Server_seq.jpg)
![alt text](../res/Server_channel_seq.jpg)
![alt text](../res/Server_DB_erd.png)

* geonwule 서버설계 문서: https://gitmind.com/app/docs/foiur01z
---
